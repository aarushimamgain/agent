# Path to the SQLite database file. Optional - defaults to
# data/workflow.db (relative to the project root) if unset. The directory
# is created automatically if it doesn't exist.
DB_PATH=

# Port the Express server listens on.
PORT=3000
